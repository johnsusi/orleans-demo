syntax = "proto3";

package domain;

message DevicePropertyValue {
    oneof value_oneof {
        string stringValue = 1;
        uint64 uintValue = 2;
        double doubleValue = 3;
    }
}

message DeviceTelemetry {    
    string name = 1;
    repeated DevicePropertyValue values = 2;
}